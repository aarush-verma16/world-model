# World Model Docs

MkDocs site stub. Architecture, concepts, experiment log, and results will land here
as the implementation progresses. See `PROJECT_BRIEF.md` for the full project plan.

Target runtime is Windows + CUDA (RTX 5080). Experiment history, including the
Mac MPS era and the Windows migration, lives in `experiments.md`.
