# Install torch/torchvision FIRST from the CUDA 12.8 index (see README).
# `pip install torch` from PyPI is CPU-only on Windows and will skip the GPU.
gymnasium==1.3.0
crafter==1.8.3
minigrid==3.1.0
PyYAML==6.0.3
tensorboard==2.21.0
mkdocs-material==9.7.7
numpy==2.4.4
pillow==12.2.0
matplotlib
